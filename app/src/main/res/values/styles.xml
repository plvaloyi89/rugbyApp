<resources>
    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.MaterialComponents.Light.Bridge">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/thirdColor</item>
        <item name="colorAccent">@color/thirdColor</item>
        <item name="bottomNavigationStyle">@style/Widget.MaterialComponents.BottomNavigationView.Colored</item>
        <item name="colorPrimary">@color/secondaryDarkColor</item>
        <item name="colorOnPrimary">@color/thirdColor</item>
    </style>

</resources>